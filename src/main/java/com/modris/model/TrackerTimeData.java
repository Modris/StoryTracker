package com.modris.model;

public class TrackerTimeData {
/*	@Id
	Long id;
	
	@OneToOne(fetch = FetchType.LAZY)
	@MapsId
	private Tracker tracker;
	
	
	
	@Formula("(SELECT timestampdiff(MINUTE, t.last_modified, current_timestamp()) FROM Tracker t WHERE t.id = id)")
	private Long lastReadMinutes;
	
	@Formula("(SELECT timestampdiff(HOUR, t.last_modified, current_timestamp()) FROM Tracker t WHERE t.id = id)")
	private Long lastReadHours;
	
	@Formula("(SELECT timestampdiff(DAY, t.last_modified, current_timestamp()) FROM Tracker t WHERE t.id = id)")
	private Long lastReadDays;
	
	@Formula("(SELECT timestampdiff(WEEK, t.last_modified, current_timestamp()) FROM Tracker t WHERE t.id = id)")
	private Long lastReadWeeks;
	
	*/
	
}
/*
notes_id smallint UNSIGNED,
created_on DATETIME,
last_read_hours DATETIME,
last_read_days DATETIME,
last_read_days_hours DATETIME,
last_read_months DATETIME,
last_read_months_days DATETIME,
last_read_years DATETIME,
*/